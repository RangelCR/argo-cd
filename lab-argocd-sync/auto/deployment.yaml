apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab-nginx-auto
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lab-nginx-auto
  template:
    metadata:
      labels:
        app: lab-nginx-auto
    spec:
      containers:
        - name: nginx
          image: nginx:1.25