apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab-nginx-full
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lab-nginx-full
  template:
    metadata:
      labels:
        app: lab-nginx-full
    spec:
      containers:
        - name: nginx
          image: nginx:1.25